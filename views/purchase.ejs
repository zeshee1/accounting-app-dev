<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Purchase System</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.3.0/css/bootstrap.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f7f6;
            margin: 20px;
        }
        .container-fluid {
            background-color: #fff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        h2 {
            color: #333;
            margin-bottom: 25px;
            text-align: center;
        }
        .form-label {
            font-weight: bold;
        }
        .item-row .form-label {
            font-size: 0.85rem;
        }
        .btn-success {
            background-color: #28a745;
            border-color: #28a745;
        }
        .btn-danger {
            background-color: #dc3545;
            border-color: #dc3545;
        }
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <h2 class="mb-4">Generate Purchase Invoice</h2>

        <form id="purchaseForm" class="mb-4">
            <div class="row g-3 mb-4">
                <div class="col-md-6">
                    <label for="registrationCode" class="form-label">Customer Registration Code:</label>
                    <div class="input-group">
                        <input type="text" class="form-control" id="registrationCode" required>
                        <button class="btn btn-primary" type="button" id="fetchCustomerBtn">Fetch Customer</button>
                    </div>
                </div>
                <div class="col-md-6">
                    <label for="customerName" class="form-label">Customer Name:</label>
                    <input type="text" class="form-control" id="customerName" readonly>
                    <input type="hidden" id="customerId">
                </div>
                <div class="col-md-6">
                    <label for="customerEmail" class="form-label">Customer Email:</label>
                    <input type="email" class="form-control" id="customerEmail" readonly>
                </div>
                <div class="col-md-6">
                    <label for="customerPhone" class="form-label">Customer Phone:</label>
                    <input type="tel" class="form-control" id="customerPhone" readonly>
                </div>
            </div>
              <div class="col-md-4">
                    <label for="nifDniType" class="form-label">NIF/DNI Type:</label>
                    <select class="form-select" id="nifDniType">
                        <option value="">Select Type</option>
                        <option value="CIF">CIF</option>
                        <option value="NIE">NIE</option>
                        <option value="NIF">NIF</option>
                        <option value="Passport">Passport</option>
                        <option value="TIE">TIE</option>
                    </select>
                </div>
                <div class="col-md-4">
                    <label for="buyerName" class="form-label">Buyer:</label>
                    <input type="text" class="form-control" id="buyerName" value="Ali Carlos" readonly>
                </div>
                <div class="col-md-4">
                    <label for="weigherName" class="form-label">Weigher:</label>
                    <input type="text" class="form-control" id="weigherName" placeholder="Enter Weigher's Name">
                </div>
                <div class="col-md-4">
                    <label for="vehicleType" class="form-label">Vehicle Type:</label>
                    <input type="text" class="form-control" id="vehicleType" readonly
                        placeholder="Auto-filled from customer details">
                </div>
                <div class="col-md-4">
                    <label for="comment" class="form-label">Comment (Optional):</label>
                    <textarea class="form-control" id="comment" rows="1" placeholder="Add any comments"></textarea>
                </div>
                <div class="col-md-4">
                    <label for="customerBehaviour" class="form-label">Customer Behaviour (Optional):</label>
                    <textarea class="form-control" id="customerBehaviour" rows="1"
                        placeholder="Describe customer's behaviour"></textarea>
                </div>
                <div class="col-md-12">
                    <label for="consentText" class="form-label">Consent (Optional):</label>
                    <textarea class="form-control" id="consentText" rows="2"
                        placeholder="Enter consent details or a statement for customer agreement."></textarea>
                </div>

            <hr>

            <h4>Items Purchased from Customer:</h4>
            <div id="itemsContainer"></div>
            
            <button type="button" class="btn btn-secondary mt-3" id="addItemBtn">Add Item</button>

            <div class="row mt-4">
                <div class="col-md-4 offset-md-8 text-end">
                    <label class="form-label h5">Total Amount:</label>
                    <input type="text" class="form-control h5" id="grandTotal" value="0.00" readonly>
                </div>
            </div>

            <div class="mt-5">
                <h5>Customer Signature (Optional):</h5>
                <canvas id="signaturePad" width="400" height="150" style="border:1px solid #ccc;"></canvas>
                <div class="mt-2">
                    <button type="button" class="btn btn-sm btn-warning" id="clearSignature">Clear</button>
                </div>
            </div>

            <div class="text-center mt-5">
                <button type="submit" class="btn btn-success btn-lg" id="generateInvoiceBtn">Generate Purchase Invoice</button>
            </div>
        </form>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/signature_pad@4.0.0/dist/signature_pad.umd.min.js"></script>
    <script src="/purchase.js"></script>
</body>
</html>
